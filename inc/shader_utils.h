#pragma once

#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <sstream>
#include <GL/glew.h>

GLuint LoadShaders(const char *vertexShader, const char *fragmentShader);
